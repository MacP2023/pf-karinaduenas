import { ConcatColsPipe } from './concat-cols.pipe';

describe('ConcatColsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatColsPipe();
    expect(pipe).toBeTruthy();
  });
});
